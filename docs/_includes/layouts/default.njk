---
title: FP
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ '/fonts.css' | url }}" />
    <link rel="stylesheet" href="{{ '/index.css' | url }}" />
    <script src="{{ '/index.js' | url }}" defer async></script>
  </head>
  <body>
    {{ content | safe }}
  </body>
</html>
