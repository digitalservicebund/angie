pre-commit:
  parallel: true
  commands:
    prettier:
      run: npx prettier --write {staged_files} && git add {staged_files}
    stylelint:
      glob: "*.scss"
      run: npx stylelint {staged_files}
