import { Canvas, Controls, Meta, Source } from "@storybook/addon-docs/blocks";
import * as RadioStories from "./Radio.stories";

<Meta of={RadioStories} />

# Radio

## Usage notes

Use a group of radio buttons when **users can only select one option** from a list.

Please also refer to [GOV.UK Design System documentation on radio buttons](https://design-system.service.gov.uk/components/radios/)
for more information.

## Example

<Canvas of={RadioStories.Default} />
<Controls of={RadioStories.Default} />

## Variants

### Small size

<Canvas of={RadioStories.Small} />

### Mini size

<Canvas of={RadioStories.Mini} />

## States

### Checked

<Canvas of={RadioStories.Checked} />

### Disabled

<Canvas of={RadioStories.Disabled} />

### Checked and disabled

<Canvas of={RadioStories.CheckedDisabled} />

## Implementation notes

- add `ds-radio` as base class to a `input[type=radio]` element
- combine with one size modifier: `ds-radio-small` or `ds-radio-mini`
- important: `input` element must be followed by `label` element in the markup
- If the label text is too long to fit on one line,
  wrap the radio and label in a container (e.g. a `div` or a `span`) containing nothing else.
  This will prevent the label text from wrapping under the radio.
  Note that this uses the `has` selector, which is only available on
  recent versions of Chrome and Safari for now. If you need to support
  Firefox or older browsers, add the `flex` class to the wrapping container instead.

<Source of={RadioStories.Small} />

Please check out the code on the given examples and otherwise
refer to [mdn web docs on \<input type="radio"\>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/radio)
for more technical information.
